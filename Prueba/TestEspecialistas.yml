info:
  name: TestEspecialistas
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/api/v1/especialistas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
