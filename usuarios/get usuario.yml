info:
  name: get usuario
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/usuarios
  body:
    type: json
    data: "\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
