info:
  name: Post roles
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/roles
  body:
    type: json
    data: |-
      {
        "nombre":"admin",
        "descripcion": "admin sistema"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
